// (() => function sayHi(str:string) {
//     return "Hello " + str;
// })();
(function () {
    function sayHi(str) {
        return "Hello " + str;
    }
    let str = 'JWnMing';
    console.log(sayHi(str));
})();
